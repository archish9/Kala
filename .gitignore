# Reference projects — upstream copies kept locally for harvesting, not our source.
# See docs/superpowers/specs/*-kala-mcp-design.md §10 for what gets ported in.
/impeccable-main/
/ui-ux-pro-max-skill-main/
/design-motion-principles-main/

node_modules/
dist/
*.log
node_modules/
dist/
*.tsbuildinfo

# tsc -b emits here; it must never sit beside sources or vitest
# resolves ../foo.js to a stale build instead of the TypeScript.
dist/
docs/
# Machine-specific: the MCP entry point is an absolute path.
# Documented in Documentation/01-getting-started.md instead.
.mcp.json


note.txt